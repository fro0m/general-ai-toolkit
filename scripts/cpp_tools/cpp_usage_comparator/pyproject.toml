[tool.poetry]
name = "cpp-usage-comparator"
version = "0.1.0"
description = "Compares C++ function and variable usage between two directories."
authors = ["Gemini"]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.8"
libclang = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
